/* Storybook Ember Sky: React is the picture frame; the game canvas is the entire experience. */
import GameCanvas from "./components/GameCanvas";
export default function App() { return <GameCanvas />; }
